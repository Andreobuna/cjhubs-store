{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "npm install --ignore-scripts",
  "buildCommand": "DATABASE_URL=\"${DATABASE_URL:-postgresql://localhost:5432/cjhubs}\" npm run prisma:generate && npm run build",
  "outputDirectory": ".next"
}
